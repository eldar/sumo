define([
    "dojo",
    "dijit/_Widget",
    "dijit/_Templated"
], function (dojo, _Widget, _Templated) {

    return dojo.declare([_Widget, _Templated], {
        widgetsInTemplate: true
    });
});
